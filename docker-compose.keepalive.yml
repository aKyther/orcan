# Local-only overlay for `orcan up` without --with-ttyd.
# Supervisord keeps the container up (keepalive) + background workers;
# use `orcan enter` for a host terminal.
services:
  orcan:
    command: ["orcan-supervisord"]
    restart: unless-stopped
    environment:
      ORCAN_SUPERVISOR_MODE: keepalive
