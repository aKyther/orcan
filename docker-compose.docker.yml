# Optional overlay: mount the host Docker socket.
# Warning: this gives the container control over the host Docker Engine.
services:
  cind:
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    group_add:
      - ${DOCKER_GID:-999}
