{
  "_comment": "Opt-in — Orcan does not add this automatically. Copy the \"hooks\" block below into this workspace's own .claude/settings.json (session-wide) or ~/.claude/settings.json (every workspace) to enable batched, automated Context Assertion drafting. See docs/en/ideas/context-assertions.md.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "orcan-context-reflect",
            "async": true
          }
        ]
      }
    ]
  }
}
