---
description: Validate changes before claiming success (project-specific)
alwaysApply: true
---

# Validation

- Run this project's available checks after meaningful changes.
- Prefer native commands (`make`, test runners, linters) defined in the repo.
- In the final report, separate what ran, what did not run, and environment limits.
- Do not claim a build or test passed unless the command actually ran.
